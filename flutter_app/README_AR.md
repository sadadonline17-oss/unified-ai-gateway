# Unified AI Gateway - APK المدمج

## نظرة عامة

تم دمج مستودع `unified-ai-gateway` بالكامل في تطبيق Flutter APK واحد. يوفر التطبيق وصولاً مجانياً إلى 30+ نموذج LLM سحابي عبر Ollama Cloud API.

## المميزات

### 🌐 30+ نموذج سحابي مجاني
- **Qwen3.5** (397B) - الأفضل للمحادثة
- **DeepSeek-V3** (671B) - ممتاز للبرمجة
- **GLM-5** (744B) - الاستدلال والوكلاء
- **Kimi-K2.5** - متعدد الوسائط
- **Llama3.2** - سريع وفعال

### 📱 مميزات التطبيق
- واجهة Flutter حديثة
- Terminal مدمج
- Dashboard ويب
- خدمة خلفية (Foreground Service)
- إدارة الطاقة عبر WakeLock

### 🔌 API Endpoints

**HTTP (Port 18789):**
- `GET /status` - حالة النظام
- `POST /ai/chat` - محادثة
- `POST /ai/code` - إنشاء كود
- `POST /ai/advanced_code` - كود متقدم
- `GET /models` - قائمة النماذج

**WebSocket (Port 18790):**
- اتصال مباشر للـ streaming

## التثبيت

### التحميل

```bash
# من GitHub Releases
https://github.com/sadadonline17-oss/unified-ai-gateway/releases
```

### البناء من المصدر

```bash
# 1. استنساخ المستودع
git clone https://github.com/sadadonline17-oss/unified-ai-gateway.git
cd unified-ai-gateway

# 2. تشغيل скриبت البناء
chmod +x build-apk.sh
./build-apk.sh

# 3. أو البناء اليدوي
cd flutter_app
flutter pub get
flutter build apk --release
```

## الاستخدام

1. **تثبيت APK** على جهاز Android
2. **فتح التطبيق** والنقر على "Begin Setup"
3. **انتظر اكتمال الإعداد** (يحمل Ubuntu, Node.js, Ollama)
4. **اسحب النماذج** من شاشة Models
5. **اختر وضع AI** (محادثة، كود، أو متقدم)
6. **ابدأ الاستخدام!**

## البنية المعمارية

```
┌─────────────────────────────────────────┐
│         Flutter App (Dart)              │
│  ┌──────────┐ ┌──────────┐             │
│  │Dashboard │ │ Terminal │             │
│  └────┬─────┘ └────┬─────┘             │
│       │             │                   │
│  ┌────┴─────────────┴────┐             │
│  │   Native Bridge (Kotlin) │           │
│  │  GatewayService          │           │
│  └────────────┬────────────┘             │
└───────────────┼─────────────────────────┘
                │
┌───────────────┼─────────────────────────┐
│  proot-distro │  Ubuntu                 │
│  ┌────────────┴────────────┐            │
│  │  Node.js 22             │            │
│  │  ┌──────────────────┐   │            │
│  │  │ Unified Gateway  │   │            │
│  │  │ - HTTP :18789    │   │            │
│  │  │ - WS   :18790    │   │            │
│  │  └──────────────────┘   │            │
│  └─────────────────────────┘            │
└─────────────────────────────────────────┘
                │
              Internet
                │
┌───────────────┼─────────────────────────┐
│     Ollama Cloud API (Free)             │
│  - 30+ free models                      │
└─────────────────────────────────────────┘
```

## الملفات المدمجة

```
flutter_app/assets/gateway/
├── lib/
│   ├── gateway/
│   │   └── unified-gateway.js
│   ├── ollama/
│   │   └── index.js
│   ├── installer.js
│   ├── bionic-bypass.js
│   └── test.js
├── bin/
│   └── unified-ai
├── package.json
└── start-gateway.sh
```

## متطلبات النظام

| المتطلب | التفاصيل |
|---------|----------|
| **Android** | 10 أو أحدث (API 29) |
| **التخزين** | ~500MB |
| **RAM** | 2GB+ موصى بها |
| **Internet** | مطلوب للنماذج السحابية |

## التطوير

### تشغيل الاختبارات

```bash
# اختبارات Node.js
cd unified-ai-gateway
npm test

# اختبارات Flutter
cd flutter_app
flutter test
```

### التصحيح

```bash
# سجلات Gateway
adb logcat | grep -i "unified\|gateway"

# سجلات Flutter
flutter logs
```

## الترخيص

MIT License - راجع LICENSE للتفاصيل.

## المساهمة

المساهمات مرحب بها! يرجى:
1. Fork المستودع
2. إنشاء فرع الميزة (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## الدعم

للأسئلة والمشاكل:
- GitHub Issues: https://github.com/sadadonline17-oss/unified-ai-gateway/issues
- المناقشات: https://github.com/sadadonline17-oss/unified-ai-gateway/discussions

---

**صنع بـ ❤️ لمجتمع Android AI**
